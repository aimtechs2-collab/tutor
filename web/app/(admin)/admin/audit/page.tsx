import AdminStubPage from "@/components/admin/AdminStubPage";

export default function AdminAuditPage() {
  return (
    <AdminStubPage
      title="Audit Log"
      description="Searchable admin action history including suspensions, plan changes, and role updates."
    />
  );
}
